{
  "name": "kithulgala-backend",
  "version": "1.0.0",
  "description": "KithulGala Adventures – REST API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seedAdmin.js",
    "migrate:r2": "node scripts/migrateLocalUploadsToR2.js",
    "test:smtp": "node -e \"const { verifySmtpConnection } = require('./services/emailService'); verifySmtpConnection().then((ok) => { process.exit(ok ? 0 : 1); }).catch(() => process.exit(1));\""
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.864.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.5",
    "validator": "^13.15.35",
    "xss-clean": "^0.1.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
